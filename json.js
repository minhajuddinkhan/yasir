//javascript object notation

// json = key value pair.

let a = { key: 0 };

if(a.key){
    console.log()
}

// null is not a JSON
// every object is not JSON
// objects that have key value pair are JSON.


